# 富途牛牛即時數據橋接 — 安裝說明

讓「窩 Trading 大腦」(`wo_trade.html`) 直接用你**已付費的富途/moomoo 即時報價與實際持股**做分析。
橋接程式 `futu_bridge.py` **只讀資料、不下單、只綁定本機、需 token**，安全。

> 目前支援**美股**即時數據（富途主力市場為美股/港股/A股；台股之後走免費公開資料）。

---

## 一、安裝 FutuOpenD（富途官方閘道）
1. 到富途官網下載 **FutuOpenD**（OpenAPI 閘道程式），安裝後用你的富途帳號登入。
2. 確認它在跑，預設連接埠 `11111`。
3. 你的帳號需有**美股即時報價權限**（你付費的那個），API 才拿得到即時價。

## 二、安裝 Python 套件
```bash
pip install futu-api
# 或
pip install -r requirements.txt
```

## 三、啟動橋接
```bash
python futu_bridge.py
```
啟動後會印出類似：
```
本機網址 : http://127.0.0.1:8888
Token    : xxxxxxxxxxxxxxxxxxxx
```
**把 Token 複製起來。**

> 若你的券商不是港股 Futu，改用 `--firm`：
> `python futu_bridge.py --firm FUTUINC`（美 moomoo）/ `FUTUSG` / `FUTUAU`

## 四、連上網頁
兩種方式擇一：

**A. 最簡單（同源、免設定）**
直接用瀏覽器開 `http://127.0.0.1:8888` —— 橋接程式會把網頁一起送出，報價/持股自動連線。

**B. 用 GitHub Pages 版**
開你部署的 `wo_trade.html`，點右上 **⚙ 設定**：
- 勾選「啟用富途牛牛即時數據」
- 橋接位址填 `http://127.0.0.1:8888`
- Token 貼上剛剛印出的字串
- 儲存

連上後，標題列會顯示 **🟢 富途**，右側「💼 持股」分頁會出現你的部位，AI 的每次分析也會自動帶入你的即時報價、持股與購買力來給部位/停損建議。

---

## 常見問題
- **標題顯示 ⚪ 未連線**：確認 `futu_bridge.py` 正在跑、FutuOpenD 已登入、位址與 token 正確。
- **報價是延遲的**：代表富途帳號該市場沒有即時權限，請在富途 App 開通。
- **持股是空的**：確認 `--firm` 對應你的開戶券商；持股查詢為只讀，不需解鎖交易。
- **https 頁面連不到 127.0.0.1**：改用方式 A（直接開 `http://127.0.0.1:8888`）。

## 安全
- 程式只開放 `127.0.0.1`，外部連不進來。
- 資料端點需 token；`.futu_bridge_token` 檔請勿外流。
- 全程**沒有任何下單/改單功能**，最多只能讀你的資料。
