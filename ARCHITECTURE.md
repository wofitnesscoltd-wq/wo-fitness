# 窩 Trading 大腦 — 世界頂尖短線決策系統 · 架構藍圖

> 定位：一套**輔助人（不自動下單）、由 AI 與量化引擎驅動、會自我驗證與自我進化**的短線交易決策系統。
> 服務對象：短線槓桿（融資/當沖/隔日沖）＋盈餘轉現股短線，美股為主。
> 核心信念：**先活下來，再求贏；程序重於結果；有量化的邊際才出手。**

---

## 0. 設計哲學（頂尖操盤手原則 — 全系統都遵守）

1. **風險優先，不是訊號優先**：先決定「能虧多少」，再談「可能賺多少」。每筆都先有停損才有進場。
2. **不對稱**：只做風險報酬比 ≥ 2 的機會；小虧多次、大賺幾次。
3. **環境決定打法**：趨勢做突破、盤整做均值回歸、高波動/risk-off 縮手降槓桿。同一招在錯的環境是反指標。
4. **程序重於結果**：好程序也會虧、爛程序也會賺；用「是否遵守計畫」評分，不是只看當天損益。
5. **邊際會衰退**：任何訊號用久會失效；持續量測、汰換、進化。
6. **人在迴路**：系統提供邊際與紀律，**最後一鍵是人**。永不自動下單。
7. **資料誠實**：拿不到的資料不假裝（不做未來函數、不用不存在的歷史）。寧可窗短而真，不要長而假。

---

## 1. 系統分層架構

```
L1 資料層      牛牛即時(報價/1分K/持股) ＋ 日線歷史 ＋ 事件/財報日曆
              (選配) Polygon歷史盤中 · 期權異常流 · 新聞流
              → 全部寫入「點時間(point-in-time)」資料庫
L2 特徵層      指標(RSI/MACD/EMA/VWAP/ATR/RVOL) ＋ 市場狀態分類 ＋ 相對強度排名 ＋ 多週期結構 ＋ 流動性
L3 訊號層      買/賣規則庫 ＋ 等級評分 ＋ 多週期匯流 ＋ 依市場狀態選策略
L4 AI複核層    Claude 對每則訊號做全情境審核(新聞/大盤/板塊) → 判讀＋精修計畫
L5 風控紀律層  部位(波動×信心) · 組合熱度 · 單日虧損上限 · 相關性 · 槓桿上限 · 事件封鎖 · 過熱偵測
L6 推播/介面   Telegram(即時) ＋ App警示欄 ＋ 績效儀表板
L7 記錄/結果層 不可竄改警示日誌 ＋ 結果追蹤(MFE/MAE/命中) ＋ 交易紀錄 ＋ 損益
L8 驗證/學習層 回測(日線長窗+盤中近窗+選配Polygon) ＋ 前向紙測 ＋ 統計歸因 ＋ 走動式優化 ＋ 冠軍/挑戰者
L9 可靠性層    心跳監控 · 斷線/當機自我警示 · 資料健檢
```

---

## 2. 訊號庫

### 2.1 買點（掃關注清單）— 環境 ＋ 觸發 ＋ 確認 ＋ 風險
觸發類型：開盤區間突破(ORB)、VWAP 回踩守住、均線多頭回測、突破前高/盤整、RSI 超賣翻揚/背離、MACD 金叉上穿零軸、相對強度領先、催化劑 gap-and-go。

### 2.2 賣點（盯持股）
觸發停損、跌破 VWAP/EMA、達標/移動停利、動能衰竭(超買+空背+量竭)、MACD 死叉、相對轉弱、事件臨近、大盤轉 risk-off 全面降槓桿。

### 2.3 訊號等級（A/B/C）＝ 條件匯流數 × RVOL × 與環境/相對強度一致性
每則必附**逐項佐證數據**＋等級，讓人能自行判斷。

### 2.4 多週期匯流（頂尖設定的關鍵）
高週期定方向(日線趨勢) → 中週期看結構(15分) → 低週期抓觸發(5分/1分)。三層同向 = 高分；背離 = 降級或不發。

### 2.5 依市場狀態適配（補上的頂尖層）
- 趨勢盤：偏突破/順勢；盤整盤：偏均值回歸；高波動：縮部位、放寬停損、降頻；risk-off：只發 A 級或關閉做多。

---

## 2B. 多流派集成引擎（各流派 ＋ 依勝率/期望值的動態權重 ＋ 綜合）

### ⚠️ 先糾正一個會害人爆倉的觀念：純用「勝率」加權是錯的
**勝率 ≠ 賺錢。**
- 趨勢/動能：**勝率低（35–45%）但賺賠比高 → 期望值正**。
- 均值回歸：**勝率高（60–70%）但有尾部風險**，一次大虧吃光多次小贏。

→ 若純照勝率加權，會**過度押注均值回歸、低估趨勢** = 散戶爆倉的典型死法。
**正確做法**：用 **期望值（勝率 × 賺賠比）＋ 跨環境穩健度** 加權，且權重**隨市場狀態動態調整**。
**最關鍵**：以下權重只是「**先驗（起手值）**」，真正權重由我們系統**用實際結果（分環境的勝率與期望值）自己學出來** —— 這才是「不是亂給」。

### 各流派 · 證據 · 角色 · 初始權重（短線美股）

**① 主訊號群（觸發，權重加總=100%）**
| 流派 | 證據強度 | 適用 | 初始權重 |
|---|---|---|---|
| 趨勢/動能（含相對強度、ORB 動能） | 強（最穩健的市場異常之一） | 趨勢盤 | **25%** |
| 突破（帶量、波動擴張） | 中強（需量能與環境確認） | 趨勢/催化 | **20%** |
| VWAP/量價（Wyckoff、VSA） | 中強（VWAP 是機構基準，盤中真實有效） | 全天 | **18%** |
| 均值回歸（短期反轉、RSI 極端） | 中（限盤整/低波動） | 盤整盤 | **12%** |
| 支撐壓力/價格行為 | 中（關鍵價位有自我實現性） | 全天 | **10%** |
| 均線系統 | 中（當濾網/風控優於當訊號） | 全天 | **8%** |
| 季節性（月底、節日、類股季節） | 弱（小邊際，當微調） | 長週期 | **3%** |
| 波浪/道氏/江恩/ICT-SMC | 弱～極弱（主觀、樣本外難重現） | — | **合計 4%（僅參考）** |

**② 確認/加成群（乘數，不是獨立觸發）**
- **相對強度因子**：高權重加成（只做比大盤/類股強的）。
- **期權異常流/籌碼**（有資料時）：高權重加成（smart money）。
- **情緒/部位**（put/call、AAII、Fear&Greed）：中權重**反向**修正（極端時逆勢提醒）。

**③ 閘門群（不加權，是門檻；不過就壓低總分或不發）**
- **總經/跨市場 regime（市場溫度）**：risk-off 直接壓低做多總分。
- **流動性**：量太小直接濾掉。
- **事件/財報**：二元事件前封鎖進場。

**④ 基本面**：短線只當**催化劑**（財報、指引、升降評），不參與技術評分。
**⑤ 機器學習/模式辨識**：對散戶過擬合風險高 → **極低權重**，僅當輔助、不主導。

### 綜合方式（集成 → 單一信心分數）
1. **閘門先過**（regime / 流動性 / 事件）——不過直接降級或不發。
2. **主訊號群**各自評分 × 權重 → 加權合計。
3. **確認群**乘數加成（相對強度、期權流）。
4. **情緒群**在極端時做反向修正。
5. 合成 **「進場信心分數」＋ 等級（A/B/C）** → 餵進 L4 AI 複核。

### 動態再加權（接 §5 優化迴圈）
每個流派的每則訊號都**獨立記錄結果** → 算各流派「**分環境的實際勝率與期望值**」 → 每週用 **走動式（walk-forward）** 更新權重：**表現衰退的降權、回升的升權（冠軍/挑戰者）**。
→ 系統越跑，權重越貼近「在當前市場、對你的標的、真正有效」的配置。

---

## 3. AI 複核層（每則都過濾）
觸發後，Claude 以**全情境**審核：大盤環境、所屬板塊強弱、即時新聞/催化劑、財報距離、相對強度 → 輸出 **進場 / 觀望 / 不建議 ＋ 理由 ＋ 精修的進場/停損/目標/部位**。
- 兩段式：技術觸發**先秒記錄**，AI 複核完才推播（確保精準）。
- 節流：同檔同訊號一場一次；高頻時優先複核高等級。

---

## 4. 風控與紀律層（注入操盤手思維 — 你沒提、但最重要）
- **部位 = f(風險% × 停損距離 × ATR × 信心等級)**：波動大自動減碼，不是固定股數。
- **單筆風險上限**（如帳戶 1%）、**單日最大虧損熔斷**（觸及今日停手）、**組合總熱度上限**。
- **相關性/集中度**：避免「10 檔都是半導體＝其實是一注」，超限警示。
- **槓桿上限**：依市場溫度動態（進攻可高、防禦強制降）。
- **事件封鎖**：財報/FOMC 前不發進場、提醒持股降槓桿。
- **過熱/上癮偵測**：偵測報復性交易、過度交易、破自己規則 → 警示喊停。

---

## 5. 結果蒐集 ＋ 自我進化迴圈
1. **發出(T0)**：存全快照（價/量/RVOL/VWAP/RSI/MACD/ATR/大盤/板塊/相對強度/新聞/AI判讀/建議進出場）。
2. **追蹤**：先碰目標(命中)或停損(未命中)？記 **MFE/MAE**、各時間窗報酬。
3. **結算**：命中/未命中/中性 ＋ 實際 R 倍數 ＋ 是否符合通知所述的點。
4. **失敗歸因(AI)**：大盤反轉？無量？撞壓力？消息逆轉？等級偏低？時段不對？
5. **彙總**：依 訊號類型/等級/板塊/環境/時段 算 勝率、期望值、平均R、盈虧比、獲利因子。
6. **走動式優化(walk-forward)**：樣本內調參、樣本外驗證；**冠軍/挑戰者**——新規則先影子跑、贏過現役才上線。
7. **定期報告**：每日收盤＋每週，AI 產「績效＋優化建議」推給你；每次規則變更都留版本與績效歸因。

---

## 6. 驗證／回測方法論
**三條腿（缺一不可）：**
- **A 歷史日線回測（10–20 年）**：波段邏輯＋環境閘門＋相對強度。資料：Futu 日線(免費)。
- **B 盤中回測（近 1–2 年）**：ORB/VWAP/RVOL。資料：Futu 可得盤中。
- **C 即時前向紙測**：盤中＋消息訊號的黃金標準（從上線第一天累積真實 track record）。

**嚴謹度**：不可未來函數（point-in-time）、樣本外驗證、計入滑價/手續費/當沖稅、用當時成分股避免存活者偏誤、參數穩健性測試（不過度優化）。

**老實的資料限制**：真·20 年「盤中＋歷史新聞」回測，免費資料辦不到；新聞層只能前向累積。要更深的盤中回測 → 選配 Polygon（近 ~10 年 1 分K）。

---

## 7. 資料庫 Schema（點時間、不可竄改）
- `alerts`：id、時間、標的、方向、訊號類型、等級、**觸發時全快照(JSON)**、AI判讀、建議進場/停損/目標/部位。
- `outcomes`：alert_id、追蹤窗、先命中或先停損、MFE、MAE、各窗報酬、結算結果、R。
- `trades`（人工/自動同步持股）：進出場、損益、R、**計畫遵守度評分**、情境快照。
- `rules_versions`：規則版本、參數、上線時間、對應績效（邊際歸因）。
- 全部本機儲存（SQLite），可重算、可稽核。

---

## 8. 推播與介面
- **Telegram**（即時、無限、免費、瀏覽器關著也收）：每則含等級＋佐證＋AI判讀＋計畫。
- **App 內**：警示欄＋音效、績效儀表板（勝率/期望值/各設定表現/優化建議）、組合熱度圖、事件日曆。
- **分流**：緊急(即時) vs 每日摘要；安靜時段；等級門檻（如 risk-off 只推 A 級）。

---

## 9. 可靠性與安全
- **心跳/看門狗**：引擎或牛牛斷線 → **主動警示你**（不能讓你以為在跑、其實掛了）。
- 資料健檢（價量異常、停牌、跳空偵測）。
- 全本機、token 控管、金鑰不入庫不外傳、只讀不自動下單。

---

## 10. 開發路線圖
- **Phase 0**：自動更新框架 ✅（已完成：橋接送出最新網頁＋自動帶 Token）
- **Phase 1 看盤地基**：自製即時圖(RSI/MACD/MA/VWAP)、部位計算機、財報/事件日曆
- **Phase 2 警示引擎**：特徵引擎＋訊號庫＋等級＋AI複核＋Telegram＋App警示欄
- **Phase 3 結果與紀律**：結果追蹤資料庫＋風控紀律層＋績效儀表板
- **Phase 4 驗證**：日線回測＋盤中回測（選配 Polygon）＋前向紙測
- **Phase 5 進化**：統計歸因＋走動式優化＋冠軍/挑戰者＋定期 AI 報告

---

## 11. 資料來源與成本
| 層 | 免費（現有） | 付費（選配，你說花費不在意可加） |
|---|---|---|
| 即時報價/盤中K/持股 | 牛牛(已付費) | — |
| 日線歷史(長窗) | Futu / Stooq | — |
| 盤中歷史(多年) | 近1-2年(Futu) | Polygon 近~10年 1分K (~US$30-200/月) |
| 新聞/催化劑 | AI 網路搜尋(即時) | 歷史新聞庫(昂貴，多半略過，靠前向累積) |
| 期權異常流/籌碼 | — | Polygon/其他(進階 edge) |
| AI 大腦 | 你的 Claude 金鑰(按量) | — |

---

## 12. 我補上的「頂尖層」（填補你想法的缺口）
1. **風控紀律層**：部位用波動×信心、單日熔斷、組合熱度、相關性、槓桿上限、事件封鎖、過熱偵測。
2. **市場狀態適配**：依趨勢/盤整/高波動/risk-off **切換策略與部位**。
3. **多週期匯流評分**：高中低週期同向才高分。
4. **邊際衰退管理 ＋ 冠軍/挑戰者**：規則持續汰換進化，新規則先影子驗證。
5. **走動式/樣本外驗證 ＋ 存活者偏誤/滑價**：讓回測不自欺。
6. **系統自我監控**：斷線/當機主動警示，不讓你盲飛。
7. **交易紀律評分**：評「有沒有照計畫做」，不是只看損益。

---

*本文件為系統源頭真理，每次架構/規則變更都應更新並記錄版本。*
