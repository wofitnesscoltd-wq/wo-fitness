# 對話提取_2026-07-01_客服模型版本查詢與repo定位

> 稽核員：環境暨進度稽核員（自動產生）
> 產生日期：2026-07-01
> 稽核對象：Claude Code 對話（主題＝查證客服模型版本 + 定位「數位客服」真正所在的 repo）
> 敏感值一律以 `[需從密碼管理器取得]` 表示。

---

## 🔴 兩道必須先讀的邊界（會影響你怎麼用這份檔案）

1. **我不在你的舊電腦上。** 這個 session 跑在**雲端臨時容器**，容器啟動時對 `wo-fitness` 做了一次全新 `git clone`。
   → 我跑的 `git status` / `node -v` / `pip` 等，反映的是**容器**，不是你要重置的那台筆電。
   → **你舊電腦上任何「沒 commit / 沒 push」的孤兒工作，這個 session 一律偵測不到。** 孤兒工作必須在**舊電腦本機**跑指令才驗得到。

2. **我的 GitHub 權限鎖死在 `wo-fitness` 一個 repo。** 已實測：讀 `wofitness`、`wofitness-linechat` 的 commit 會回 `Access denied`。
   → 你要找的「數位客服」**不在** `wo-fitness`（見下），而那兩個 repo 我在這個 session 讀不到內容，只能拿到公開 metadata。

---

## 一、真實時間戳記（來源：實際指令 `git log --all`）

```
ad40141 | 2026-04-27 01:15:39 +0800 | Rename wo_admin (2).html to wo_admin.html
bafa629 | 2026-04-27 01:03:00 +0800 | Add files via upload
```
- 整個 `wo-fitness` repo 只有 **2 筆 commit**，全部在 2026-04-27。
- 之後唯一的變動是本次稽核建立的分支 `claude/fable5-model-version-check-nb1jo0`（尚未 push）。

---

## 二、這個對話的工作進度

1. **主題/任務**：使用者聽說「Fable 5 被關掉」，要確認客服用什麼模型；接著發現找不到「數位客服」，轉為定位它真正在哪個 repo。
2. **起點**：從零。repo 內**無 CLAUDE.md**（實際指令確認），故無專案記憶可接續。
3. **實際變更**：僅**新增分析檔案**（`進度快照_*.md`、本檔），**未改動任何產品程式碼**，未 commit，未部署。
4. **結論狀態**：
   - ✅ 已查明：`wo_staff_query.html:627`、`wo_teacher_schedule.html:828` 皆用 `claude-sonnet-4-20250514`（Claude Sonnet 4），與 Fable 5 無關。
   - ✅ 已查明：`wo-fitness` 內**不存在**任何「數位客服」頁面（含 git 歷史）。
   - ✅ 已定位：使用者名下有 **3 個 repo**，數位客服極可能在 `wofitness-linechat` 或 `wofitness`（見第三節 A）。
   - ⏳ 卡住點：無權限讀那兩個 repo，需 `add_repo`（本 session 不可用）或改在綁定那些 repo 的 session／本機進行。
5. **關鍵決策與理由**：不靠傳聞/記憶判斷，一律以 git 時間戳、磁碟檔案、GitHub metadata 為準（source of truth）。
6. **未完成事項/下一步**：把「數位客服」的對賬移到 `wofitness-linechat`（首選）與 `wofitness`（次選）。
7. **信心分數**：**高**（針對 `wo-fitness` 的結論皆由實機指令佐證；針對另兩 repo 僅能到 metadata 層級，內容未讀）。
8. **撞名風險標註**：本對話主題與截圖中「數位改革」session 相關，但**操作的是不同 repo**——本對話＝`wo-fitness`，數位改革＝`wofitness`（私有）。屬【不同 repo，非重工】。

---

## 三、環境與設定

### A. 專案與程式碼（來源：實際指令 + GitHub metadata）
使用者名下 **3 個 repo**：

| Repo | 用途研判 | 最後更新 | 私有 | 本 session 可讀? |
|---|---|---|---|---|
| `wofitnesscoltd-wq/wo-fitness` | 3 個靜態 HTML（員工查詢/老師時間/規則後台） | 2026-07-01(本次稽核) | 否 | ✅ 內容全可讀 |
| `wofitnesscoltd-wq/wofitness` | 截圖「數位改革」session 綁定的 repo | 2026-06-30 | 🔒 是 | ❌ Access denied |
| `wofitnesscoltd-wq/wofitness-linechat` | **LINE 聊天機器人**（名稱直指數位客服/`wo_linechat_v*.js`） | 2026-06-22 | 否 | ❌ Access denied |

- `wo-fitness` remote：`…/git/wofitnesscoltd-wq/wo-fitness`（HTTPS，經 local_proxy）。
- 分支：`main`、`claude/fable5-model-version-check-nb1jo0`（後者本次新建，未 push）。
- `.gitignore`：**無**（實際指令確認）。
- 使用模型：`claude-sonnet-4-20250514`（Claude Sonnet 4），寫死於兩個 HTML 前端 `fetch` 內。

### B. Cloudflare（來源：實際指令）
- `wo-fitness` **非 Cloudflare 專案**：無 wrangler、無 `wrangler.toml/jsonc`、無 `.wrangler/`。
- 若有 Worker/D1/KV/Secrets/Cron，**應在 `wofitness-linechat`**（本 session 讀不到，待該 repo 對賬）。

### C. GitHub
- Owner：`wofitnesscoltd-wq`。PAT/SSH 權限範圍：`[需從密碼管理器取得]`。
- Actions/CI：`wo-fitness` 內無 `.github/workflows`（僅 3 個 HTML 檔）。

### D. 憑證與金鑰
- `wo-fitness` 前端直接呼叫 `api.anthropic.com` → 需 **Anthropic API key**：`[需從密碼管理器取得]`。
  ⚠️ 註：金鑰若寫在前端 HTML 會外洩；本次未逐行讀取金鑰存放方式，**建議檢查並輪替**。
- LINE channel access token / secret / webhook URL：`[需從密碼管理器取得]`（預期在 `wofitness-linechat`）。

### D-2. 帳號歸屬對照表
- 本 session 無法查各服務註冊身分/2FA/救援碼綁定裝置——這些**必須你本人**在密碼管理器/各後台確認。逐項填：Cloudflare / GitHub / LINE Developers / 網域商 / Google / Anthropic / 恆木科技 → 註冊身分(個人 or 窩 WoFitness)、註冊 email、存取權(你/靜秋)、2FA 綁哪支裝置、救援碼位置、⚠️ 是否綁在舊電腦本機。

### E. 開發環境
- ⚠️ **以下為「雲端容器」數值，非你舊電腦**（來源：實際指令）：node v22.22.2、npm 10.9.7、Python 3.11.15；全域套件含 `@anthropic-ai/claude-code@2.1.42`、playwright、prettier 等（容器預裝，非你的設定）。
- **你舊電腦的實際 node/wrangler/python/VS Code 擴充/MCP 設定，須在舊電腦本機跑一次提示詞 1 才抓得到。**

### F. 自動化與整合
- `wo-fitness` 內無自動化。Make/Zapier、LINE OA webhook、POS(恆木科技) 整合狀態：`[待 wofitness-linechat 對賬]`。

### G. 三方備份架構
- 無法從本 session 判定。需你確認三份正本位置與同步方向。

### H. 容易漏掉
- `.env`/`.dev.vars`/`.wrangler`：`wo-fitness` **皆無**（實際指令）。
- webhook URL、OAuth app、SSH key 位置、shell alias：本 session 不可見，須本機確認。

---

## 四、⚠️ 本對話沒涵蓋到的項目（缺漏清單）

1. **`wofitness-linechat` 的全部內容**（最可能是「數位客服」本體）——本 session 無權限，未讀。
2. **`wofitness`（私有）的全部內容**——「數位改革」session 的成果，未讀。
3. **舊電腦本機的孤兒工作**——雲端容器偵測不到，**換機前必須在舊電腦本機自行檢查**。
4. **各線上服務的 2FA/救援碼綁定裝置**——只有你本人查得到。
5. **前端 API 金鑰是否外洩／需輪替**——建議實際打開 HTML 檢查金鑰存放方式。

### 下一步建議（依序）
1. 在**綁定 `wofitness-linechat` 的 Claude Code session**（或本機 clone 後）跑提示詞 1 → 那才是數位客服正本。
2. 對 `wofitness`（私有）同樣做一次。
3. **重置電腦前**，在舊電腦本機執行：`git status`、`git stash list`、`git log --branches --not --remotes` → 清掉所有孤兒工作並 push。
4. 三份提取檔收齊後跑提示詞 2 合併。
